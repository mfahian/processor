server:
  port: 8087

feign:
  client:
    config:
      ModelConnector:
        loggerLevel: full
      CatalogConnector:
        loggerLevel: full

connectors:
  model:
    url: http://localhost:8086
  catalog:
    url: http://localhost:8085

logging:
  level:
    com:
      malpro:
        processor: debug