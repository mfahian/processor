spring:
  rabbitmq:
    host: localhost
    password: guest
    username: guest
    port: 5672
    listener:
      simple:
        default-requeue-rejected: false
rabbitmq:
  store-product:
    queue: store-product.q
connectors:
  model:
    url: localhost:8110
  catalog:
    url: localhost:8100