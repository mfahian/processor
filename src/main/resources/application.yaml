spring:
  rabbitmq:
    host:
    password:
    username:
    port:
    listener:
      simple:
        default-requeue-rejected: false

rabbitmq:
  store-product:
    exchange:
    queue:
    dead-letter-queue:

connectors:
  model:
    url:
  catalog:
    url: